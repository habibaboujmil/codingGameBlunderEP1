export type State = {
  x: number;
  y: number;
  inverted: boolean;
  breakerMode: boolean;
  direction: string;
};

export type Teleporter = {
  x: number;
  y: number;
}

export type Point = {
  x: number;
  y: number;
}